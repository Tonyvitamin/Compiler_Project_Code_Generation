PROGRAM foo(input, output, error) ;
   // variable declaraions
   var a : integer;

	  
begin   
      a := 1;
      while a < 5 do
      begin
		 writeln(a);
         a   := a + 1 
      end;
	  
end.   // this is the end of the program
